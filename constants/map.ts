// Fallback map center (London, Ontario) used when location permission is
// denied or unavailable, so the map still has somewhere sensible to open.
export const DEFAULT_MAP_REGION = {
  latitude: 42.9849,
  longitude: -81.2453,
  latitudeDelta: 0.08,
  longitudeDelta: 0.08,
};
